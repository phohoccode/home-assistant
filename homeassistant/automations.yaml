
# Automation 1: Event Trigger (Fast) - Nhận trực tiếp từ MQTT
- id: '1001'
  alias: Fast MQTT Response
  description: 'Phản hồi nhanh khi nhận MQTT message'
  trigger:
      - platform: mqtt
        topic: zigbee/zigbee_temp_sensor_01/state
  action:
    - service: notify.persistent_notification
      data:
        title: "⚡ Fast MQTT - {{ now().strftime('%H:%M:%S.%f')[:-3] }}"
        message: >
          Temperature: {{ trigger.event.data.payload_json.temperature }}°C
          Humidity: {{ trigger.event.data.payload_json.humidity }}%
          Battery: {{ trigger.event.data.payload_json.battery }}%

# Automation 2: State Trigger (Slow) - Đợi State Machine cập nhật
- id: '1002'
  alias: Slow State Response
  description: 'Phản hồi qua State Machine'
  trigger:
    - platform: state
      entity_id: sensor.zigbee_temperature_sensor_living_room_temperature
  action:
    - service: notify.persistent_notification
      data:
        title: "🐌 Slow State - {{ now().strftime('%H:%M:%S.%f')[:-3] }}"
        message: >
          Temperature: {{ states('sensor.zigbee_temperature_sensor_living_room_temperature') }}°C
          Humidity: {{ states('sensor.zigbee_temperature_sensor_living_room_humidity') }}%